before_script:
    - env | sort

.deploy_templ: &deploy_templ
  stage: deploy
  script:
    - sudo rm -rf /opt/waqf
    - sudo mkdir -p /opt/waqf
    - sudo cp -rf . /opt/waqf
    - sudo cp .env.examlpe /opt/waqf/.env
    - cd /opt/waqf
    - export PWD=$(pwd)
    - echo $PWD
    - sudo chown -R 1000:1000 /opt/waqf
    - docker-compose -f docker-compose-server.yml up -d --build
    - echo 'Successfully deploy'

deploy:
  <<: *deploy_templ
  tags:
    - shell-dev
  only:
    - develop
    - devops/cicd
